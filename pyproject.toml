[project]
name = "mist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets",
    "matplotlib>=3.10.8",
    "modal",
    "tiktoken",
    "tinygrad",
]
